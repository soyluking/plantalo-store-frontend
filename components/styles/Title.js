import styled from 'styled-components';

const Title = styled.h3`
  font-size: 1.15rem;
  font-weight: 400;
  margin: 0.5rem 1rem;
  text-align: center;
  a {
    color: var(--grey);
    display: block;
    line-height: 1.4;
    text-align: center;
    padding: 0 1rem;
  }
`;

export default Title;
